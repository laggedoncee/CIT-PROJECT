kind = "web"
previewPath = "/"
title = "Discover Tirana"
version = "1.0.0"
id = "artifacts/tirana"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22778

[services.development]
run = "pnpm --filter @workspace/tirana run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/tirana", "run", "build" ]
publicDir = "artifacts/tirana/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22778"
BASE_PATH = "/"
